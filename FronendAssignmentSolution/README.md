- Install json-server npm module:
    npm install -g json-server
    npm install -g http-server

 - Open command prompt in json-server folder
 and run "json-server --watch server.json"

 - Go into FrontendSolution folder and run 
 http-server